---
apiVersion:
kind: 
metadata:
  name: 
  labels:
    app: 
spec: 
  replicas: 
  selector:
    matchLabels:
      app:  
  template:
    metadata:
      labels:
        app: 
    spec: 
      containers:
      - name: 
        image:  
        ports:
        - containerPort: 

--- 
apiVersion:
kind: 
metadata:
  name: 
spec:
  selector:
    app: 
  type:   
  ports:
    - protocol: 
      port: 
      targetPort: 
      nodePort: 
...               
